{
    "$id": "https://vng.brp.nl/schemas/kind.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "version": "0.1.0",
    "title": "Kind",
    "description": "Gegevens over een kind van de persoon.",
    "type": "object",
    "properties": {
        "burgerservicenummer": {
            "type": "string",
            "example": "555555021"
        },
        "leeftijd": {
            "type": "integer",
            "description": "Leeftijd op het moment van bevragen",
            "example": 12
        },
        "inOnderzoek": {
            "$ref": "#/definitions/KindInOnderzoek"
        },
        "naam": {
            "$ref": "#/definitions/Naam"
        },
        "geboorte": {
            "$ref": "#/definitions/Geboorte"
        }
    }
}